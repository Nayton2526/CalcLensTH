# CalcLens TH Android V0.1

Installable Android wrapper for the offline CalcLens TH V0.1 calculation engine.

The GitHub Actions workflow builds a debug-signed APK that can be installed directly on Android.

APK output: `app/build/outputs/apk/debug/app-debug.apk`
